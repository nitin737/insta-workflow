# Workspace Rules

- Always validate the spec file (such as `docs/specs/automation_spec.md`) with the current code and check if it is in sync before proceeding with related tasks.
- For any new integration, create a documentation file (e.g., `docs/<integration-name>-setup.md`, like `gemini-setup.md` for Gemini AI integration) detailing the high-level changes made.
